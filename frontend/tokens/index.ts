const colors =  require('./colors.ts')
const spacing =  require('./spacing.ts')
const borderRadius =  require('./borderRadius.ts')
const fontSize = require('./fontSize.ts')
const fontFamily = require('./fontFamily.ts')
const fontWeight = require('./fontWeight.ts')

module.exports = {
  colors,
  spacing,
  borderRadius,
  fontSize,
  fontFamily,
  fontWeight
}